# Version notes

This starter intentionally uses the current Tauri 2 architecture and a conservative browser target for Catalina.

The exact latest package versions may change. When compiling later, the coding agent should resolve compatible Tauri/React/Vite/Rust versions rather than blindly upgrading across major versions.

The macOS bundle sets `minimumSystemVersion` to `10.15`.
